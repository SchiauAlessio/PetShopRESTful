package core.model.validators;

import core.model.exceptions.ValidatorException;
import core.model.OrderProduct;

public class OrderProductValidator implements Validator<OrderProduct> {
    @Override
    public void validate(OrderProduct orderProduct) throws ValidatorException {

    }
}
